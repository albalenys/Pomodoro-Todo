<main>
  <div class="clock-info">
    <h3>Start Timer</h3>
    <%= form_for @pomodoro, url: task_pomodoros_path(@task), :html => {id: "start"} do |f|%>
      <%= f.text_field :length, id: "length", placeholder: "length of time" %><br>
      <%= f.hidden_field :task_id, value: @task.id %><br>
      <%= f.hidden_field :text, id: "text", value: @task.text %>
      <%= f.submit "Submit", class: "btn btn-info" %>
    <% end %>
  </div>

  <%= image_tag("pomodoro-timer.png", class: "clock-bg") %>

  <div class="clock">
    <span class="minutes"></span>:<span class="seconds"></span>
  </div>
</main>